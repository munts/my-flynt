{% extends 'templates/_document.twig' %}

{% block content %}
    {% for component in post.meta('pageComponentsAccommodations') %}
        {{ renderComponent(component) }}
    {% endfor %}
{% endblock %}
